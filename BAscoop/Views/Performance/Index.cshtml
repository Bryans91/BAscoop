@model IEnumerable<BAscoop.Models.Performance>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Cinemaroom.name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Movie.title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.StartTijd)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Cinemaroom.name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Movie.title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.StartTijd)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PerformanceId }) |
            @Html.ActionLink("Details", "Details", new { id=item.PerformanceId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PerformanceId })
        </td>
    </tr>
}

</table>
